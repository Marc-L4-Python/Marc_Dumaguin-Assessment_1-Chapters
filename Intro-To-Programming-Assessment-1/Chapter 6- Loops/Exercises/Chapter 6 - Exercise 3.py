a = 1

while a < 2:
    print (a)


# A code that is infinite. 
# The loop has a condition that if "a" is less than 2, then it will print itself - it will print the variable "a".
# Since the condition of the loop is true "forever" (until the value of "a" is changed), it will keep on printing the
# variable "a", making a loop.