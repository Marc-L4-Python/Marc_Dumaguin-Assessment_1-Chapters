a = "First String"
b = "Second String"
c = "Third String"

print("       ".join([a,b,c]))


# This code has 3 variables that has strings as values and then prints them all together as 1 string.
# The ".join" function combines the 3 strings into 1 rather than printing the variables together,
# as that prints them as a "variable" rather than as a "string".